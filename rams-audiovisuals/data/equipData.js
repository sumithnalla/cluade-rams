// Canonical equipment data currently lives in `claudeData.js`.
// This re-export keeps the rest of the site using the expected `equipData.js` entry point.
export { equipment } from './claudeData.js';
